[build]
builder = "nixpacks"
buildCommand = "pip install playwright==1.49.0 && playwright install chromium --with-deps"

[deploy]
startCommand = "python bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
